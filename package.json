{
  "name": "poc-app",
  "version": "1.0.0",
  "description": "POC application — triggers playwright-automation tests on each deploy",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "start:dev": "NODE_ENV=dev node src/index.js",
    "start:staging": "NODE_ENV=staging node src/index.js",
    "start:prod": "NODE_ENV=prod node src/index.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
